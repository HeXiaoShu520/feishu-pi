# 公司网络常见 TLS 中间代理（自签根证书）会让 Node 只认内置 CA 而握手失败
# （UNABLE_TO_VERIFY_LEAF_SIGNATURE）。npm 把 node-options 注入 lifecycle 脚本的
# NODE_OPTIONS，npm start / npm run setup 及其派生的 lark-cli 子进程因此改用
# 操作系统证书库。绕过 npm 直接跑（tsx src/main.ts、IDE 调试）时需自行设置
# NODE_OPTIONS=--use-system-ca；低版本 Node 用 NODE_EXTRA_CA_CERTS 指向公司根证书。
# 不要用 NODE_TLS_REJECT_UNAUTHORIZED=0（完全关闭校验）。
node-options=--use-system-ca
