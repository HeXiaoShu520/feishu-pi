Node.js ESM + TypeScript (target ES2022, noEmit)；运行时通过 `tsx` 直跑 TS；HTTP 框架使用 Express 5 + body-parser；飞书 SDK 为 `@larksuiteoapi/node-sdk`；Agent 核心依赖 `@earendil-works/pi-*` 系列；定时任务使用 `croner`；配置注入使用 `dotenv`；测试使用 `vitest`。