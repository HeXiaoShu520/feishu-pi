`npm run start` 通过 `tsx src/main.ts` 启动主服务；`npm run config` 单独启动 127.0.0.1:3456 的配置界面；`npm run dev` 使用 `tsx watch` 热重载；`npm test` 执行 `vitest --run --config vitest.config.ts`；`npm install` 后自动执行 `scripts/patch-pi-ai.js` 修补第三方包。