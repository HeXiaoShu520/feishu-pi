`npm run start` 通过 tsx 直接执行 `src/main.ts`；`npm run dev` 使用 `tsx watch` 热重载；`npm run config` 单独启动配置页面服务器；`postinstall` 自动执行 `scripts/patch-pi-ai.js` 对 pi-ai 进行补丁；测试通过 `vitest --run --config vitest.config.ts` 运行。