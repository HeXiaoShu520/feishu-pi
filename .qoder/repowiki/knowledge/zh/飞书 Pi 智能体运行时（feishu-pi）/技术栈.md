TypeScript (ESM) + tsx 运行；飞书官方 `@larksuiteoapi/node-sdk` 底层 WSClient 直连事件总线；智能体内核使用 `@earendil-works/pi-coding-agent`、`@earendil-works/pi-ai`；Express + body-parser 提供配置/统计页面；croner 实现定时任务；Vitest 单元测试。