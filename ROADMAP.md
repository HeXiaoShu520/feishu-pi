# feishu-pi 开发路线

## 1. 产品目标

feishu-pi 最终要成为一个飞书原生的轻量 Agent 后端：

```text
飞书消息
  → 会话路由与上下文恢复
  → Pi Agent
  → 内置工具与飞书业务工具
  → 记忆与经验沉淀
  → 飞书文本 / CardKit 回复
```

核心目标：

- 让用户可以在飞书中进行稳定的多轮对话。
- 让 Agent 能够安全调用文件、命令和飞书业务工具。
- 让会话、记忆和工具调用具备可追踪、可恢复、可控制的生命周期。
- 保持单一飞书场景的轻量性，不提前引入多渠道 Gateway 的复杂度。

## 2. 当前状态

### 已完成

- [X] 基于 Pi 公开 SDK 创建 Agent Session。
- [X] 接入 `pi-coding-agent`、`pi-agent-core` 和 `pi-ai`。
- [X] 启用 `read`、`write`、`edit`、`bash` 内置工具。
- [X] 支持注入自定义 `AgentTool`。
- [X] 接入飞书官方 Node SDK WebSocket Channel。
- [X] 实现飞书消息到 Pi Prompt 的转换。
- [X] 按 `chatId` 和 `threadId` 隔离会话。
- [X] 保证同一会话内消息串行处理。
- [X] 支持不同会话并行处理。
- [X] 实现飞书引用回复和占位消息编辑。
- [X] 合并高频文本更新，保证回复写入顺序。
- [X] 建立 TypeScript 检查和 Vitest 测试配置，并确认测试用例实际执行。
- [X] 删除旧 Python miniClaw 和桌面宠物代码，统一为根目录 Node.js 工程。

### 当前限制

- [ ] 尚未完成真实飞书和真实模型 smoke test。
- [ ] Session 文件尚未与飞书 `conversationId` 建立持久映射。
- [ ] 自定义工具注册仍有运行时类型断言。
- [ ] 目前使用普通文本编辑，不是 CardKit Producer 流。
- [ ] 尚未接入长期记忆、Skills 和飞书业务工具。
- [ ] 尚未实现断线重连和完整优雅关闭。

## 3. 分阶段计划

### Phase 1：身份与权限基础

目标：先建立所有技能共享的飞书基础上下文，以及会话和权限基础。

任务：

- [X] 获取并标准化用户 Open ID、英文名和部门信息；首次聊天保存到 `memory/users/`。
- [X] 定义所有技能共享的 `FeishuContext`。
- [ ] 按用户或群生成稳定的会话 ID。
- [ ] 定义人员、部门、技能和工具的权限配置。
- [ ] 在技能和 Agent 工具调用前执行权限检查。
- [ ] 支持技能根据部门或身份选择不同子分支。

验收标准：

- 每条消息都能关联到明确的用户 Open ID、英文名和部门。
- 所有技能都能读取同一份 `FeishuContext`。
- 不同用户或群的会话不会互相混淆。
- 未授权技能或工具调用会被拒绝并给出可理解的提示。
- 同一技能能根据部门或身份进入预期子分支。

### Phase 2：真实运行闭环

目标：证明飞书、身份、模型、Pi Agent 和回复更新能够稳定连通。

目标：证明飞书、模型、Pi Agent 和回复更新能够稳定连通。

任务：

- [ ] 配置真实模型 Provider 和认证。
- [ ] 启动真实飞书 WebSocket 连接。
- [ ] 验证普通文本多轮对话。
- [ ] 验证 `read`、`write`、`edit`、`bash`。
- [ ] 验证模型工具调用期间的飞书回复更新。
- [ ] 补充启动错误和模型错误日志。

验收标准：

- 飞书消息能够触发 Agent 回复。
- Agent 能正确执行四个内置工具。
- 回复更新不重复、不乱序。
- 工具失败时用户能收到明确错误提示。

### Phase 3：会话可靠性

目标：让会话在并发、重启和重复消息场景下保持正确。

任务：

- [ ] 建立 `conversationId → sessionFile` 持久映射。
- [ ] 服务启动时恢复已有会话。
- [ ] 完善消息去重和重复投递处理。
- [ ] 实现 WebSocket 断线重连和退避。
- [ ] 实现 SIGINT/SIGTERM 优雅关闭。
- [ ] 为会话数量和空闲时间增加清理策略。

验收标准：

- 服务重启后同一飞书会话能够恢复上下文。
- 同一消息重复投递不会重复执行 Agent。
- WebSocket 短暂断开后能够自动恢复接收。
- 关闭服务时不丢失正在写入的 Session 数据。

### Phase 4：飞书业务工具

目标：把原 miniClaw 的有效 Function Calling 迁移为清晰、可测试的 Pi 工具。

任务：

- [ ] 定义业务工具的命名、参数、返回值和错误规范。
- [ ] 正式适配 `AgentTool` 到 Pi 的 `ToolDefinition`。
- [ ] 迁移飞书文档读取和写入能力。
- [ ] 迁移多维表格能力。
- [ ] 增加日历和审批能力。
- [ ] 增加工具级权限和操作确认。
- [ ] 为每个工具增加 mock 测试和真实 API 测试。

验收标准：

- 每个工具都能被 Agent 正确发现和调用。
- 参数错误、权限错误和 API 错误都能结构化返回。
- 高风险写操作具备明确的权限边界或确认机制。
- 业务工具不依赖旧 Python 模块。

### Phase 5：记忆与 Skills

目标：在不污染短期上下文的前提下提供可控的长期能力。

任务：

- [ ] 区分会话上下文、用户记忆和工作区记忆。
- [ ] 定义记忆写入、读取、更新和删除接口。
- [ ] 将经过筛选的记忆注入 Agent 上下文。
- [ ] 增加经验沉淀规则，避免保存无价值对话。
- [ ] 增加 Skills 目录和加载协议。
- [ ] 限制记忆体积、敏感信息和注入频率。

验收标准：

- 用户明确要求保存的信息可跨会话使用。
- 无关聊天不会无限写入记忆。
- 记忆内容可人工查看、修改和删除。
- 记忆不会突破模型上下文限制或覆盖系统约束。

### Phase 6：飞书交互体验

目标：使用飞书原生卡片完善长文本和交互式工作流。

任务：

- [ ] 接入 CardKit Producer 式流式回复。
- [ ] 支持处理中、成功、失败状态。
- [ ] 支持 Markdown、图片、按钮和选择控件。
- [ ] 支持工具确认和多步任务交互。
- [ ] 处理长文本、频率和消息大小限制。
- [ ] 增加 CardKit 生命周期和异常恢复测试。

验收标准：

- 流式卡片更新稳定且按顺序到达。
- Agent 工具确认可以在飞书卡片中完成。
- 长文本不会导致消息失败或界面不可用。
- CardKit 异常时能够退回普通文本回复。

## 4. 优先级原则

开发顺序固定为：

```text
身份与权限基础
  → 真实运行闭环
  → 会话可靠性
  → 业务工具迁移
  → 记忆与 Skills
  → CardKit 体验
```

不在前置阶段引入：

- 多渠道支持。
- 独立 CLI 或 TUI。
- 向量数据库和复杂检索平台。
- 未被业务验证的 Agent 自主编排。
- 对 Pi 核心源码的 fork 或修改。

## 5. 每个阶段的交付要求

每完成一个阶段，必须同时具备：

- 代码实现。
- 针对核心行为的自动化测试。
- 更新架构文档和本路线图。
- 明确的手工验收步骤。
- 已知限制和回滚方式。

阶段完成以验收标准为准，不以代码提交数量或模块数量为准。
